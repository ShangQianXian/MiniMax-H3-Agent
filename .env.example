# MiniMax 开放平台配置
# 在 https://platform.minimaxi.com/user-center/basic-information/interface-key 获取普通开放平台 API Key
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.cn

# 本地服务
PORT=8787
HOST=127.0.0.1

# 设为 1 时不调用真实接口，回放固定响应，便于离线开发与测试
MOCK_H3=0

# SQLite 数据库与素材/产物落盘目录（相对仓库根目录）
DATA_DIR=data
